[project]
name = "zayd-service-retrieval"
version = "0.0.0"
requires-python = "==3.12.3"
dependencies = [
  "zayd-common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zayd_service_retrieval"]
