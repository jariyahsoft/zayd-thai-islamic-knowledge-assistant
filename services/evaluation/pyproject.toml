[project]
name = "zayd-service-evaluation"
version = "0.0.0"
requires-python = "==3.12.3"
dependencies = [
  "zayd-common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zayd_service_evaluation"]
