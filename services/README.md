# Services

Owner category: backend maintainers.

Services contain API, orchestration, retrieval, ingestion, worker, and evaluation runtime code. Services may use shared contracts and SDK interfaces, but provider-specific implementations belong under `plugins/`.
