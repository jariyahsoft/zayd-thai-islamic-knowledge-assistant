# Plugins

Owner category: provider integration maintainers.

Plugins implement provider and storage interfaces from the SDK packages. Plugin loading must use an explicit allow-list and must not execute arbitrary runtime code.
