# Packages

Owner category: shared platform maintainers.

Shared TypeScript packages expose contracts, UI primitives, configuration, generated clients, provider interfaces, plugin interfaces, and test utilities. Packages must not depend on app or service layers above them.
