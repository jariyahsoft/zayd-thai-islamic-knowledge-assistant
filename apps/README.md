# Apps

Owner category: frontend maintainers.

Applications provide user, reviewer, and admin interfaces. Apps may depend on shared TypeScript packages and generated API clients, but they must not connect directly to PostgreSQL, Redis, object storage, or provider SDK implementations.
